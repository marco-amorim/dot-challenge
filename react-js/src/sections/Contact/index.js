import React from 'react';
import Form from '../../components/Form';
import './styles.css';

const Contact = () => {
	return (
		<section id="contact">
			<svg className="contact__diagonal__bg"></svg>

			<Form />
		</section>
	);
};

export default Contact;
